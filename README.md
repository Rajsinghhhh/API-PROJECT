# API-PROJECT
Task Manager API is a RESTful backend service that allows users to securely manage their tasks. It provides authentication using JWT and supports full CRUD (Create, Read, Update, Delete) operations for task management.
